[Journal]
# Sailfish ships Storage=volatile, so anything that goes wrong before a reboot
# leaves nothing to read afterwards -- a boot failure or a wedged wizard is
# unreportable. For an alpha port that is the wrong trade, so keep logs on disk
# with a small cap.
Storage=persistent
SystemMaxUse=60M
